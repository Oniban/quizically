# Optional: same Google OAuth web client ID as server GOOGLE_CLIENT_ID.
# Leave empty to use email/password authentication only.
# VITE_* values are public build-time values, never secrets.
VITE_GOOGLE_CLIENT_ID=

# API URLs are hardcoded to /api. Vite proxies them locally;
# production hosting must provide a same-origin /api reverse proxy.
